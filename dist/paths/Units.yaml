get:
  summary: "Get all units"
  operationId: "getUnits"
  responses:
    "200":
      description: "List of units"
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Unit"